import { AnimTransitionRevealSimple } from './anim-transition-reveal-simple';

describe('AnimTransitionRevealSimpleDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimTransitionRevealSimple();
    expect(directive).toBeTruthy();
  });
});
